var name = prompt ("Please enter your name", "");

    if ( !( name === "" || name === "null" || name === null) ){
        alert("Welcome, " + name + ".");
        
        
    } else { 
        alert("You didn't type anything.");
    }
